# Settings

## Overview

Application preferences panel. Allows users to configure default font, theme, and auto-save behavior.

## Requirements

**settings-persist**: User preferences MUST persist across app launches.

**settings-defaults**: The app MUST ship with sensible defaults for all settings.
