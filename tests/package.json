{
  "name": "agentic-cookbook-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:static": "vitest run --project static",
    "test:smoke": "vitest run --project smoke",
    "test:evals": "vitest run --project evals",
    "test:watch": "vitest --project static"
  },
  "devDependencies": {
    "vitest": "^3.2.1",
    "yaml": "^2.7.1"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.4"
  }
}
